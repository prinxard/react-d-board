
const Centered = ({children}) => (
  <div
    data-layout="centered"
    className="w-full h-full py-4 flex items-center justify-center bg-gray-50">
    {children}
  </div>
)

export default Centered
